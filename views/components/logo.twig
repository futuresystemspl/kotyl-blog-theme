<figure class="{% if cssClass %}{{cssClass}} {% endif %}logo">
	<a href="{{ links.store }}" title="{{ site.description }}">
		{{ source('assets/svg/logo.svg', ignore_missing = false) }}
	</a>
</figure>
